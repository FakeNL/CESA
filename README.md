### CESA

#### Running
Clone the project
`git clone git@github.com:FakeNL/CESA.git`

Install bundler
`gem install bundler`

Install rails
`gem install rails`

Run migrations
`rake db:migration`

`Run the project`
`rails s`

